"""HyperMix DSP package — physical device models and production utilities.

Separation of concerns:

    MS20MFilter / ms20m_*       = physical virtual-analog device model
    linear_phase_* / oversample = transparent production utility layer
    calibration/                = hardware measurement harness
"""
